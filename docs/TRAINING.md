# Training

Training is not implemented in this phase.

Future training stages are `base_pretrain`, `fim_train`, `instruction_tune`, and `eval_only`. Every phase must include tests and must not fake success.
